# the upstream component nginx needs to connect to
upstream django {
    server unix:///home/liset/sl_app_uwsgi_nginx/shoppinglist/shoppinglist.sock;

}
# configuration of the server
server {
    listen      80;
    # server_name 192.168.1.22;
    charset     utf-8;

    # max upload size
    client_max_body_size 75M;
    # Django media and static files
    location /media  {
        alias /home/liset/sl_app_uwsgi_nginx/shoppinglist/media;
    }
    location /static {
        alias /home/liset/sl_app_uwsgi_nginx/shoppinglist/static;
    }
    # Send all non-media requests to the Django server.
    location / {
        uwsgi_pass  django;
        include     /home/liset/sl_app_uwsgi_nginx/nginx/uwsgi_params;
    }
}
